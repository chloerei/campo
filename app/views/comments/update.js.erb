<% if @comment.errors.empty? %>
  $('#comment-<%= @comment.id %>').replaceWith('<%= j render @comment %>');
  <% if @comment.liked_by?(current_user) %>
    campo.Likes.updateLike('comment', <%= @comment.id %>);
  <% end %>
<% end %>
